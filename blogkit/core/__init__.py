"""blogkit.core — the shared pipeline engine (fetch, generate, image, publish).

Profiles supply per-blog customization; everything reusable lives here so a fix
or feature lands once instead of 20 times.
"""
