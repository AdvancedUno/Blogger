"""Per-blog profile modules.

Each ``<slug>.py`` here defines a module-level ``PROFILE = BlogProfile(...)``.
The registry (``blogkit.profiles.registry``) discovers them automatically — add
a blog by dropping in a new module, no engine changes required.
"""
