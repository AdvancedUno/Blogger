"""blogkit — profile-driven automated multi-blog content pipeline.

A shared, tested core engine (fetch -> generate -> image -> publish) plus one
editable profile per blog under ``blogkit.profiles`` that customizes persona,
RSS queries, image styles, tags, and publish settings.
"""

__version__ = "0.1.0"
