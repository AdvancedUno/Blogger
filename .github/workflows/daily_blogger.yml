name: Daily Blogger Post

# Single track: Blogger v3 REST API only — no Playwright / storage_state.

on:
  schedule:
    # Daily at 02:00 UTC = 11:00 KST = 22:00 EST (prev day)
    # so the post is live for the US morning audience.
    - cron: '0 14 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Generate only — do NOT publish (verify CI env safely)'
        type: boolean
        required: false
        default: false
      disable:
        description: 'Comma-separated blog slugs to skip this run (optional)'
        required: false
        default: ''

concurrency:
  group: daily-blogger-post
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    # 20 sites x (~15s work + ~45s source-text/URL-resolve + 30s inter-site
    # sleep) ≈ 30 min; headroom absorbs multiple 65s Gemini quota retries before
    # GitHub auto-cancels mid-network.
    timeout-minutes: 120
    permissions:
      contents: read       # checkout
      id-token: write      # mint the OIDC token for keyless GCP auth (Vertex)

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      # No Playwright install step — Blogger v3 is a pure REST API.

      # Keyless Google Cloud auth (Workload Identity Federation) — only when the
      # image provider is Vertex. No long-lived JSON key; this step mints a
      # short-lived token from the GitHub OIDC token and exports
      # GOOGLE_APPLICATION_CREDENTIALS + GOOGLE_CLOUD_PROJECT for the run step.
      - name: Authenticate to Google Cloud (keyless)
        if: ${{ vars.IMAGE_PROVIDER == 'vertex' }}
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Run blogger pipeline
        env:
          # --- AI (multi-key routing, GEMINI_API_KEY is the fallback) ---
          GEMINI_API_KEY:       ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY_1:     ${{ secrets.GEMINI_API_KEY_1 }}
          GEMINI_API_KEY_2:     ${{ secrets.GEMINI_API_KEY_2 }}
          GEMINI_API_KEY_3:     ${{ secrets.GEMINI_API_KEY_3 }}
          GEMINI_API_KEY_4:     ${{ secrets.GEMINI_API_KEY_4 }}
          GEMINI_API_KEY_5:     ${{ secrets.GEMINI_API_KEY_5 }}
          GEMINI_API_KEY_6:     ${{ secrets.GEMINI_API_KEY_6 }}
          GEMINI_API_KEY_7:     ${{ secrets.GEMINI_API_KEY_7 }}
          GEMINI_API_KEY_8:     ${{ secrets.GEMINI_API_KEY_8 }}
          GEMINI_API_KEY_9:     ${{ secrets.GEMINI_API_KEY_9 }}
          GEMINI_API_KEY_10:    ${{ secrets.GEMINI_API_KEY_10 }}
          GEMINI_MODEL:       ${{ secrets.GEMINI_MODEL }}
          # --- Featured image generation (provider selected by IMAGE_PROVIDER) ---
          # IMAGE_PROVIDER is a repo *variable* (Settings -> Variables), not a
          # secret, so the auth step above can branch on it. "hf" (default,
          # free) or "vertex" (Google Cloud Imagen, paid).
          IMAGE_PROVIDER:       ${{ vars.IMAGE_PROVIDER }}
          # hf provider: Hugging Face FLUX.1-schnell token (free tier).
          HF_API_TOKEN:         ${{ secrets.HF_API_TOKEN }}
          # vertex provider: the auth step exports GOOGLE_APPLICATION_CREDENTIALS
          # + GOOGLE_CLOUD_PROJECT. These vars only override/pin the region/model.
          GCP_PROJECT:          ${{ vars.GCP_PROJECT }}
          VERTEX_LOCATION:      ${{ vars.VERTEX_LOCATION }}
          VERTEX_IMAGE_MODEL:   ${{ vars.VERTEX_IMAGE_MODEL }}
          # Optional: only set if your org permits service-account JSON keys.
          GCP_SA_KEY:           ${{ secrets.GCP_SA_KEY }}
          # PAT (contents:write on advancedunolee/blog-assets) for hosting images.
          ASSETS_REPO_PAT:      ${{ secrets.ASSETS_REPO_PAT }}
          # --- Blogger OAuth ---
          GOOGLE_CLIENT_ID:     ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
          # --- Run digest webhook (Discord/Slack-compatible; optional) ---
          RUN_WEBHOOK_URL:      ${{ secrets.RUN_WEBHOOK_URL }}
          # --- Search Console feedback (optional; needs webmasters.readonly
          #     scope. Falls back to GOOGLE_* above if these are unset) ---
          GSC_CLIENT_ID:        ${{ secrets.GSC_CLIENT_ID }}
          GSC_CLIENT_SECRET:    ${{ secrets.GSC_CLIENT_SECRET }}
          GSC_REFRESH_TOKEN:    ${{ secrets.GSC_REFRESH_TOKEN }}
          # --- Skip specific blogs this run (manual dispatch input) ---
          BLOGKIT_DISABLE:      ${{ github.event.inputs.disable }}
        # Preflight first (fail fast on missing secrets), then run.
        # Use --group N to run only one batch (e.g., `python -m blogkit run --group 2`).
        # A manual dispatch with dry_run=true generates without publishing —
        # this verifies the full CI env (pip install -e ., doctor, package
        # import, generation) safely. Scheduled runs always publish for real.
        run: |
          python -m blogkit doctor
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            echo "::notice::dry_run=true — generating without publishing"
            python -m blogkit run --dry-run
          else
            python -m blogkit run
          fi
